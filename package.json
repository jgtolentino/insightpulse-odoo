{
  "name": "insightpulse-odoo-dev",
  "version": "1.0.0",
  "description": "Development server for InsightPulse Odoo with live reload",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost --root=./addons --open=/addons",
    "start": "live-server --port=3000 --host=localhost --root=./addons",
    "watch": "live-server --port=3000 --host=localhost --root=./addons --watch=./addons"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": [
    "odoo",
    "development",
    "live-reload",
    "hot-reload"
  ],
  "author": "InsightPulse",
  "license": "MIT"
}
