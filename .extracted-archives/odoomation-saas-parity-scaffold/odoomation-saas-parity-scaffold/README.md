# Odoomation SaaS-Parity Scaffold

Production-ready scaffold to build Odoo CE + OCA–aligned SaaS parity modules and a secure API surface for agentic automation.

## Quick start

```bash
docker compose up -d --build
open http://localhost:8069
# Configure db and add addons path: /mnt/extra-addons
# Install module: Finance Expense (odoomation_finance_expense)
```
